# 通用项目协作规范

本文件适用于此仓库下由 AI 协作维护的任意项目，与语言、框架和项目类型无关。

## 版本控制

- 项目开始时确认 Git 仓库已初始化；未经要求不要重写历史或丢弃用户修改。
- 将彼此独立的改动拆成小而完整的提交；每项完成、验证后再继续下一项。
- 提交前检查暂存区。提交说明必须覆盖暂存区中的全部变更。
- 推荐使用 Conventional Commits；类型和 scope 可为英文，冒号后的主题和正文使用中文。
- 不提交可再生的构建产物、缓存、临时文件或本地配置。通过 `.gitignore` 明确忽略它们。

## 工程质量

- 构建产物应集中到独立目录（推荐 `out/` 或项目惯用目录），不要与源码混放。
- 构建规则应追踪源码和配置/头文件依赖，避免增量构建产生不一致的二进制。
- 每次功能或构建改动后，执行项目适用的构建与测试；修复警告和失败后再提交。
- 用户可见的构建、安装、运行、配置和操作方式变化时，同步更新 README。
- 涉及系统路径安装时，提供可配置的安装前缀或目标目录，并在覆盖已有文件前给出明确确认或安全的非交互选项。

## 协作与交付

- 先说明将要做什么，再执行；完成后简洁说明结果、验证方式和必要限制。
- 对可能影响用户环境的操作（例如系统安装、覆盖文件、删除文件）先确认目标和权限。
